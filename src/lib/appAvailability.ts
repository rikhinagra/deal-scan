/**
 * Flip to `true` when DealScan is live on the Apple App Store.
 * While false, the site shows only Google Play download paths and Android-focused copy.
 */
export const IOS_APP_AVAILABLE = false;
